# ZRAM build acceleration
[build]
incremental = true
target-dir = "/mnt/zram/targets/sovereign-ai-stack-book"

[alias]
t = "test"
c = "check"
b = "build --release"
cl = "clippy --all-targets -- -D warnings"
